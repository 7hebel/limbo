ctrl+down on CMD scrolls

string.ColoredStringObject function to return dict{pos:char} instaed of stream

nodes and sources documentation + help command (move some shortcuts to help command?)

static compiler executor as standalone file.

SAVE camera position in save file (however, do not make file !is_saved when shifting)

ON NODE FOCUS, if outside work rect, move camera to it

(maybe) mouse support
